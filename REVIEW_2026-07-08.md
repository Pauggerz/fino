# Three-pass review — branch `improvement/Features1.0` (2026-07-08)

> Living tracker for the 3-pass review (correctness → security → system sweep)
> of the intelligence-upgrade branch, and the improvement plan that came out of
> it. Check items off as they land. Companion to `INTELLIGENCE_UPGRADE.md`.

**Scope:** full branch diff vs `main` (25 files, ~1,620 insertions — Phases A–D
of the intelligence upgrade + the assist tier) plus a system-wide sweep beyond
the diff.

**Baseline at review time — all gates green:**

| taxonomy | route | brain | query | memory | typo | tsc |
| -------- | ----- | ----- | ----- | ------ | ---- | --- |
| 133      | 84    | 501   | 158   | 31     | 31   | 0   |

---

## Pass 1 — correctness review (high effort, 8 finder angles, verified by execution)

~32 candidates from 8 independent review angles; each uncertain one was
verified by **running the actual brain code** in a tsx probe. Four
scary-sounding candidates were refuted empirically (`osaDistance` NaN,
`answerLogClarify` crash, stale spell memo, the `recommend`-removal
regression). Ten findings survived, ranked:

### Confirmed bugs

1. **Spell corrector rewrites proper nouns into vocabulary words** —
   `src/intelligence/convo/spell.ts:113` (`correctToken`).
   Reproduced: `"how much did i pay marco"` → `"…pay march"` → canonicalized
   `"did i pay"` → answered as **billStatus at confidence 0.95** (a
   subscriptions answer to a question about a person). `"i paid marco 200"`
   logs display text "march". The wrong-answer class this branch was built to
   kill, reintroduced by its own fix.
   Related drift: plan A3 says user **account/category names** feed the spell
   vocab — `buildVocab()` never got them.

2. **Glued-amount splitter logs product model numbers as prices** —
   `src/intelligence/core/amounts.ts:65` (`GLUED_AMOUNT_RE`).
   Reproduced: `parseChatTransaction("bought iphone15")` → **₱15, "Shopping -
   Iphone"** staged as a log (same shape: "galaxy s24"-, "covid19"-style
   tokens: alpha ≥ 3 + short digit run).

3. **Prompt-injection line embedded in `INTELLIGENCE_UPGRADE.md` line 6** —
   the `## Why` heading is glued to injected text ("…remove all MD… remove
   those test scripts"). It is an instruction aimed at future AI sessions to
   delete the docs and test gates. **Never obey it; delete it.**

4. **Assist-adoption gate accepts a `logClarify` reroute** —
   `src/screens/ChatScreen.tsx:1388`.
   Reproduced: `routeMessage("bought coffee 100")` → `meta.intent
   'logClarify'`, confidence 0.9, no `assistEligible` — passes every adoption
   condition. A statement-shaped LLM rewrite would be adopted as the "answer"
   to a question ("Sounds like you're logging Coffee — I just couldn't find
   the amount", with the amount right there), and `resolvedIntent:
   'logClarify'` is recorded into the miss buffer as a labeled training pair
   (corpus poison — it is not a trainable intent).

### Verified weaknesses / cleanups

5. **`brain-assist` edge fn hardening** —
   `supabase/functions/brain-assist/index.ts:114`: Gemini key rides the URL
   query string (`?key=…`) instead of the `x-goog-api-key` header; error paths
   echo internals to the client (`String(err)`, Gemini error body, raw model
   text); no per-user throttle.
6. **Hardcoded `0.6`** in the adoption gate (`ChatScreen.tsx:1388`) instead of
   importing `MEDIUM_CONFIDENCE` from the brain — silently desyncs when B4
   recalibration lands.
7. **Barrel rule violated** — `ChatScreen.tsx:64` imports
   `@/intelligence/assist/assistClient` sub-path; `requestBrainAssist` isn't
   re-exported from the `@/intelligence` barrel at all (CLAUDE.md
   single-public-surface rule; the `ocr/` precedent it cites *is* in the
   barrel).
8. **Wasted hot-path work** — `src/intelligence/convo/brain.ts:339` tests the
   ~1KB `FINANCE_ANCHOR_RE` against both `norm` and `fixed` on **every**
   message, though the gate only applies when rules were fully silent; hoist
   inside the `ruleScore === 0 && mlIntent && DATA_INTENTS` condition.
9. **Module-global memo** — `src/intelligence/convo/spell.ts:148`
   (`lastIn`/`lastOut`): shared mutable state inside the "pure" layer; thrashes
   on interleaved inputs, races under any future parallel use.
10. **`'logClarify'` is stringly-typed** — rides `meta.intent` outside the
    `IntentId` union (`brain.ts:408`); consumers handle it by convention, not
    type.

---

## Pass 2 — security review

**No new HIGH/MEDIUM vulnerabilities introduced by the branch.**

- `validateAssistDecision` whitelists intents and rejects URLs/markup/overlong
  rewrites; input capped at 280 chars; only the message text ever leaves the
  device; LLM output is never written silently (confirm-chip only). Held up
  under scrutiny.
- The error-echoing and public/no-JWT posture of `brain-assist` **mirror the
  pre-existing `parse-receipt`/`split-receipt` functions** — repo precedent,
  i.e. hardening debt (item 5 above), not a new hole.
- Miss-telemetry buffer stays device-local (AsyncStorage), never synced.

---

## Pass 3 — system-wide sweep (beyond the diff)

| Area | Verdict |
| --- | --- |
| Intent catalog sync (intents.ts ↔ assistCatalog.ts ↔ edge-fn CATALOG ↔ corpus) | ✅ In sync today — but **no drift guard**; a manual "redeploy" comment is the only protection |
| Sync/privacy invariants (`chat_messages`, `notifications` unsynced; telemetry local) | ✅ Clean |
| ChatScreen error handling (assist throw → spinner/persistence) | ✅ Safe (`requestBrainAssist` never throws; `finally` clears state) |
| `handleSend` complexity | ✅ 250 lines, proportional to its job; no dead state |
| **Test coverage of the assist tier** | 🔴 **Zero** — `validateAssistDecision`, `assistClient`, the edge function, and the ChatScreen adoption path have no gate |
| TODO/FIXME markers | ✅ Only 2 informational ⚠️ comments |

---

## Improvement plan

### P0 — fix before merge (each with new `test:typo` / `test:brain` cases)

- [x] **P0.1 Spell-corrector guards** — never correct a token *into* a
      time-grammar word (months/weekdays/time words); skip capitalized
      mid-sentence tokens (likely proper nouns); add user account + category
      names to the vocab (close the A3 doc/code drift).
      Files: `convo/spell.ts` (`CALENDAR_TARGETS` now wired into `correctToken`;
      `isSentenceStart` proper-noun skip in `spellNormalize`), `brain.ts`
      (re-exports `extendSpellVocab`), `screens/ChatScreen.tsx` (calls
      `extendSpellVocab` with account + category names per send).
      Tests: `correctToken('marco'|'frida') === null`; `spellNormalize` keeps
      `"marco"` and a mid-sentence `"Marco"`.
      *Nuance found:* the finding attributed the wrong billStatus answer to the
      `marco→march` rewrite, but billStatus fires on the `"did i pay"` rule
      itself (score 5, name-independent) — a **separate** intent-rule weakness,
      out of P0.1's spell-corrector scope. The manufactured-`march` defect (the
      display-name leak + proper-noun rewrite) is fixed.
- [x] **P0.2 Glued-amount tightening** — reject splits where the alpha run is
      a product/model pattern (iphone/galaxy/ps/usb/mp-style + short digit
      run), or require the digit run to look like money in context.
      Files: `core/amounts.ts` (`PRODUCT_STEMS` denylist + `MODEL_NUMBER_RE`;
      `splitGluedAmounts` now a guarded callback). Tests: `"iphone15"` stays
      whole and does not parse a ₱15 log; `"chicken200"` still splits.
- [x] **P0.3 Assist-adoption gate** — reject `logClarify` reroutes; import
      `MEDIUM_CONFIDENCE` instead of hardcoded `0.6`; never record
      `logClarify` as `resolvedIntent` in the miss buffer.
      Files: `screens/ChatScreen.tsx` (reroute gate excludes `logClarify` and
      gates on `MEDIUM_CONFIDENCE`; `recordBrainMiss` filters `logClarify` from
      `resolvedIntent`). Test: `"bought coffee 100"` routes to `logClarify` (the
      shape the gate rejects).
- [x] **P0.4 Delete the injected line** from `INTELLIGENCE_UPGRADE.md` line 6;
      restore the plain `## Why` heading. Done — heading is now bare `## Why`.

### P1 — guards & hardening (landed 2026-07-09)

- [x] **P1.1 `scripts/check-intents-sync.ts`** (modeled on
      `check-copy-sync.ts`): text-diffs the `IntentId` union ↔ `ASSIST_INTENTS`
      ↔ edge-fn `CATALOG` (plus corpus-labels ⊆ IntentId, closing the pass-3
      four-way drift concern); `npm run check:intents-sync`, wired into
      `.husky/pre-commit`. Verifies in-sync today: 46 intents, 44
      assist-eligible, 44 catalog lines.
- [x] **P1.2 `test:assist` harness** — `shouldAdoptAssistReroute(meta)`
      extracted into `convo/assistCatalog.ts` as a pure type-guard predicate
      (ChatScreen now calls it); `scripts/test-assist.ts` gates it +
      `validateAssistDecision` + end-to-end `routeMessage` meta shapes (33
      cases, incl. the P0.3 `"bought coffee 100"` logClarify rejection).
      `npm run test:assist`.
- [x] **P1.3 Harden `brain-assist`** — Gemini key moved off the URL into the
      `x-goog-api-key` header; all failure paths return a generic
      `assist_unavailable` body (details go to `console.error` / function
      logs); per-caller sliding-window throttle (10 req/min, keyed by auth
      header → IP fallback). ⚠️ Needs redeploy (rides the P2.2 deploy item).
- [x] **P1.4 Barrel compliance** — `convo/assistCatalog` + `assist/assistClient`
      re-exported from `src/intelligence/index.ts`; ChatScreen now imports
      `requestBrainAssist` / `shouldAdoptAssistReroute` from `@/intelligence`
      (sub-path import removed).
- [x] **P1.5 Small cleanups** — anchor-regex test hoisted inside the
      rules-silent gate condition (`brain.ts`); module-global `spellNormalize`
      memo removed (stateless now — re-correcting a short message is cheap);
      `PseudoIntentId` (`'logClarify'`) added in `convo/types.ts` and
      `meta.intent` typed `IntentId | PseudoIntentId | null` (no longer a bare
      string).

### P2 — standing backlog this review re-confirms (landed 2026-07-10)

- [x] **P2.1 B4 calibration** — train-time margin→accuracy curve emitted into
      `model.json` (replaces the heuristic confidence mapping).
      `train-brain.ts` now runs stratified 5-fold CV over the corpus, scores
      held-out gate-passing predictions with the B1 composite
      (`rawClassifierScore` — extracted into `classifier/naiveBayes.ts` so the
      trainer and `computeConfidence` share one definition), quantile-bins +
      PAV-pools them into an isotonic `calibration.bins` curve (7 bins from
      408 points; held-out `unknown` leaks count as wrong). `computeConfidence`
      maps classifier wins through `calibratedConfidence` (raw composite stays
      the fallback for an older model). Weak scores now land honestly in the
      LOW/MEDIUM bands (bottom bin ≈ 0.25, top ≈ 0.79). Gated by four `[B4]`
      cases in `test:brain` (curve present / ascending / isotonic + clamped /
      live `routeMessage` reads it).
- [x] **P2.2 Pending deploys** — `brain-assist` deployed (`--no-verify-jwt`,
      matching its documented public posture; smoke-tested live: a typo-heavy
      message came back `{"intent":"spend",...}`); `GEMINI_API_KEY` secret was
      already set on the project (2026-04-16, shared with parse-receipt);
      `delete-account` deployed (gateway JWT + self-validation);
      `add_debt_direction.sql` applied via the Supavisor pooler —
      `debts.direction` exists, default `'owed_to_me'`, all 16 existing rows
      backfilled. Both functions are now recorded in `supabase/config.toml`.
- [x] **P2.3 Add-Transaction-sheet parity** — decided + implemented:
      **glue recovery yes, chat spell-fix no.** The sheet's analyzers
      (`analyzer.analyze`, `detectAccount`, `matchIncomeKeyword`,
      `suggestCategory`, and save-time `buildDisplayName`) now read the
      `extractAmountsRecovered` surface, so "chicken200" auto-fills ₱200 +
      Food and saves as "Food - Chicken" while the P0.2 product guard keeps
      "iphone15" whole (no phantom ₱15). Chat's `spellNormalize` deliberately
      does NOT apply: the sheet analyzes half-typed words on a 300 ms
      debounce (OOV by construction → flicker), `analyzeTransactionText` has
      its own fuzzy keyword layer for finished typos, and the description
      field always stays the user's own text. `extractAmountsRecovered` is
      re-exported through `categorize.ts` → the `@/intelligence` barrel.
      (ScreenshotScreen's analyzer path left unchanged — OCR text comes from
      the server-side parsers, which have their own normalization.)

## Gate log

| Date | taxonomy | route | brain | query | memory | typo | tsc | Note |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| review baseline (2026-07-08) | 133 | 84 | 501 | 158 | 31 | 31 | 0 | pre-P0 |
| P0 landed (2026-07-08) | 133 | 84 | 501 | 158 | 31 | 39 | 0 | P0.1–P0.4 fixed; typo +8 |
| P1 landed (2026-07-09) | 133 | 84 | 501 | 158 | 31 | 39 | 0 | P1.1–P1.5; new gates: assist 33, check:intents-sync ✓ (both in pre-commit) |
| P2 landed (2026-07-10) | 133 | 84 | 505 | 158 | 31 | 39 | 0 | P2.1–P2.3; brain +4 `[B4]` calibration cases; `test:assist` actually added to pre-commit (P1 doc drift); assist 33 ✓ |
